# This program reads in a line of space separated names, after which it prints out an English style list, i.e. a comma separated 

line = raw_input('Enter space separated names: ')

list1 = line.split(' ')

print list1
