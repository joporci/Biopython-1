# This program outputs the position of every 't' in the list. 

list1 = ['a', 'c', 'g', 't', 't', 'a', 't']

print list1.index('t')		# to check the number of t's in the list

for i in range(len(list1)):
	if list1[i] == 't':
		print i
